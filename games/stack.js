//! name=STACK color=0,255,150
async function tmp_default(api){const{setPx,drawText,drawRect,display,piezo,Effects,sleep,gameOverScreen,makeHControl,flashScreen,explode,joyX,joyY,sliderPos,held,mpu,colors}=api;const LAYER_H=4;const PALETTE=[colors.rgb(0,255,150),colors.rgb(0,200,255),colors.rgb(150,100,255),colors.rgb(255,0,120),colors.rgb(255,150,0),colors.yellow];let layers=[{x:22,w:20}];let colorOffset=0;let curW=20;let curX=0;let moveDir=1;let speed=1.1;let score=0;let dropped=false;api.dpadHandler=()=>{dropped=true};api.joyClickHandler=()=>{dropped=true};while(!api.exitRequested){curX+=moveDir*speed;if(curX<=0){curX=0;moveDir=1}if(curX>=64-curW){curX=64-curW;moveDir=-1}if(dropped){dropped=false;const below=layers[layers.length-1];const start=Math.max(curX,below.x);const end=Math.min(curX+curW,below.x+below.w);const w2=end-start;if(w2<=0){piezo.playSong(Effects.damage);await flashScreen(colors.rgb(60,0,0),1);await gameOverScreen(score);return}if(w2>=curW-.6){piezo.playSong(Effects.coin)}else{piezo.playSong(Effects.menuMove)}layers.push({x:start,w:w2});score++;curW=w2;speed+=.07;moveDir=Math.random()<.5?1:-1;curX=moveDir>0?0:64-curW;if(layers.length>11){layers.shift();colorOffset++}}display.clear();for(let i=0;i<layers.length;i++){const y=59-i*LAYER_H;drawRect(Math.round(layers[i].x),y,Math.round(layers[i].w),LAYER_H-1,PALETTE[(i+colorOffset)%PALETTE.length])}const curY=59-layers.length*LAYER_H;drawRect(Math.round(curX),curY,Math.round(curW),LAYER_H-1,colors.white);drawText(1,1,String(score),colors.rgb(70,70,70));display.show();await sleep(30)}}export{tmp_default as default};
